name: Build Docker Image

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Можно запускать вручную

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    # 1. Забрать код
    - name: 📥 Скачать файлы
      uses: actions/checkout@v4
    
    # 2. Посмотреть что есть
    - name: 📁 Проверить файлы
      run: |
        echo "Файлы в проекте:"
        ls -la
    
    # 3. Собрать Docker образ
    - name: 🔧 Собрать образ
      run: |
        docker build -t my-docker-lab-image:latest .
        echo "✅ Образ собран!"
    
    # 4. Сообщить об успехе
    - name: ✅ Готово
      run: |
        echo "🎉 Всё работает!"
        echo "Docker образ создан: my-docker-lab-image:latest"
